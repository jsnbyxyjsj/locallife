import { __Value } from '@leafer/interface';
import { IUI } from '@leafer-ui/interface';

declare function effectType(defaultValue?: __Value): (target: IUI, key: string) => void;
declare function resizeType(defaultValue?: __Value): (target: IUI, key: string) => void;

export { effectType, resizeType };
